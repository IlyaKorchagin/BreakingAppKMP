name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Show root directory files (для отладки)
        run: ls -la

      - name: Show BreakingApp directory files (для отладки)
        run: ls -la BreakingApp

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Build project (production)
        run: |
          cd BreakingApp
          ./gradlew :composeApp:jsBrowserProductionWebpack

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./BreakingApp/composeApp/build/kotlin-webpack/js/productionExecutable
